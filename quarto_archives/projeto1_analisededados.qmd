---
title: "As mudanças de hábitos alimentares na pandemia foram para bons ou maus hábitos?"
author: "Gabriel Miranda"
format: 
  html:
    toc: true
    toc-depth: 3
    theme: cosmo
    code-fold: true
    embed-resources: true
editor: visual
---

```{r echo=TRUE, warning=FALSE, message=FALSE}
library(readxl)
library(dplyr)
library(ggplot2)
library(tidyr)
library(knitr)
library(stringr)
library(gtsummary)

dados0 <- read_excel("C:\\Users\\Usuario\\Desktop\\DataProjects\\quarto_archives\\DadosNutri.xlsx")

theme_gtsummary_language("pt", big.mark = ".", decimal.mark = ",") 
```

\newpage

# Introdução

Em 2022, alunas do curso de pós-graduação em Nutrição da UFF coletam `r nrow(dados0)` respostas em um questionário online com `r ncol(dados0)` perguntas relacionadas a características sociodemográficas, condições vivenciadas durante a pandemia de COVID-19, características antropométricas e mudanças nos hábitos alimentares.

O objetivo desse estudo é, a partir da base de dados coletada, responder a seguinte pergunta: As mudanças de hábito alimentares na pandemia foram para bons ou maus hábitos?

# Dados e Variáveis Utilizadas







O que será investigado:
- As mudanças de hábito que ocorreram na pandemia 
foram para bons ou maus hábitos?



Introdução:

- Descrever a origem dos dados
-- periodo
-- individuos analisados
-- como e quando se deu a coleta dos dados
- Descrever o contexto e foco da minha analise

- Texto

Dados e Variáveis Utilizadas:

- Descrever o subuniverso que será utilizado
- Descrever quais variáveis serão utilizadas para
analisar o foco da analise

- Texto e tabela com a descritiva

Resultados:

- Gráficos e tabelas para observar as mudanças de hábitos
relatadas e a suas relações com caracteristicas 
sociodemográficas.

Principais Achados:

- Principais destaques / resumo da parte de resultados.









## Objetivos do estudo

A base de dados tem como propósito investigar as mudanças nos hábitos alimentares durante a pandemia de COVID-19 e sua possível associação com características sociodemográficas, econômicas, antropométricas e relacionadas às condições vivenciadas durante o período.

De forma específica, busca:

-   Caracterizar o perfil sociodemográfico e antropométrico dos participantes, considerando gênero, raça/cor, região de residência, escolaridade, renda familiar, idade e IMC;
-   Descrever as condições vivenciadas durante a pandemia, incluindo isolamento social, dificuldades financeiras e ocorrência de COVID-19;
-   Identificar a ocorrência de alterações gerais na alimentação durante a pandemia;
-   Examinar mudanças específicas no consumo alimentar, considerando aumento, redução ou manutenção do consumo de diferentes grupos de alimentos e bebidas;
-   Investigar associações entre as mudanças nos hábitos alimentares e as características sociodemográficas, econômicas, antropométricas e relacionadas à pandemia;
-   Compreender possíveis desigualdades nos impactos da pandemia sobre os hábitos alimentares dos participantes.

## INTRODUÇÃO E CONTEXTUALIZAÇÃO

Inicialmente, apresentem brevemente o contexto da base de dados e o problema que será investigado.

Essa parte deve permitir que uma pessoa que não conhece previamente a base compreenda:

-   de onde vieram os dados;
-   quem são os indivíduos ou unidades analisadas;
-   quando e como os dados foram coletados;
-   qual é o contexto do estudo;
-   qual será o foco da análise realizada.

A contextualização deve ser breve e direcionada ao restante da apresentação.

### OBJETIVOS DA ANÁLISE

Apresentem de forma clara o objetivo geral da análise e, quando necessário, os objetivos específicos que orientarão a apresentação.

Os objetivos deverão funcionar como um guia para a construção da narrativa.

Evitem simplesmente reproduzir todos os objetivos sugeridos na descrição da base. Será possível selecionar e priorizar determinados aspectos, desde que consiga desenvolver uma análise coerente dentro do tempo disponível.

### DADOS E VARIÁVEIS UTILIZADAS

Apresentem brevemente a base de dados e indiquem quais variáveis foram selecionadas para a análise.

Procurem distinguir, quando pertinente:

-   variáveis desfecho;
-   variáveis explicativas;
-   variáveis qualitativas;
-   variáveis quantitativas.

Não é necessário apresentar detalhadamente todas as variáveis existentes na base. Destaquem principalmente aquelas que serão importantes para compreender os resultados apresentados posteriormente.

Tabelas compactas, esquemas ou outros recursos visuais podem ser utilizados para facilitar essa apresentação.

### RESULTADOS

Esta deverá ser a parte principal da apresentação.

Os resultados deverão ser organizados de maneira lógica e conectados aos objetivos definidos anteriormente.

Utilizem adequadamente os recursos estudados na disciplina, incluindo:

-   tabelas automáticas;
-   gráficos estatísticos estáticos;
-   medidas descritivas, quando pertinentes;
-   comparações entre grupos;
-   cruzamentos entre variáveis;
-   outros recursos de análise exploratória que contribuam para a investigação.

### PRINCIPAIS ACHADOS

Ao final da apresentação, sintetizem os principais resultados encontrados.

Em vez de simplesmente repetir números apresentados anteriormente, procurem responder:

O que aprendemos com esses dados?

Destaquem os padrões, diferenças ou associações que considerarem mais relevantes para os objetivos estabelecidos.

Quando pertinente, também poderão ser mencionadas limitações importantes dos dados ou da análise realizada.

### ORGANIZAÇÃO E COMUNICAÇÃO

A qualidade do projeto não será avaliada apenas pela quantidade de técnicas utilizadas.

Será considerada especialmente a capacidade do grupo de:

-   selecionar informações relevantes;
-   construir uma narrativa coerente;
-   utilizar adequadamente tabelas e gráficos;
-   interpretar corretamente os resultados;
-   comunicar os achados de maneira clara;
-   produzir uma apresentação visualmente organizada;
-   respeitar o limite de 15 minutos.

A apresentação deverá ser planejada para o público, e não apenas como uma demonstração dos códigos desenvolvidos.

Não é necessário exibir códigos extensos durante a apresentação. O código deverá estar incorporado ao documento Quarto e ser utilizado para garantir a reprodutibilidade da análise.

```{r echo=TRUE, warning=FALSE, message=FALSE}
explicativas = c(
     'Genero'
    ,'Raca_cor'
    ,'Regiao'
    ,'Renda_familiar'
    ,'Escolaridade'
    ,'Trabalha'
    ,'Idade'
    ,'Peso'
    ,'Dificuldade_Financeira'
)
desfechos = c(
     'Alteracao_Pandemia'
    ,'Frutas'
    ,'Legumes_Verduras'
    ,'Bebidas_Alcoolicas'
    ,'Produtos_Industrializados'
    ,'Doces'
)
```

```{r, echo=TRUE, warning=FALSE, message=FALSE}
dados0 = dados0 %>%
    select(all_of(explicativas), all_of(desfechos))
```

```{r warning=FALSE, message=FALSE}
tbl_summary(
    data = dados0 %>%
      select(all_of(explicativas), "Alteracao_Pandemia"),
    by = "Alteracao_Pandemia",
    statistic = list(all_categorical() ~ "{n} ({p}%)",
                     all_continuous() ~ "{mean} ({sd})"),
    label = list(
        Genero ~ "Gênero",
        Raca_cor ~ "Raça/Cor",
        Regiao ~ "Região",
        Renda_familiar ~ "Renda familiar",
        Escolaridade ~ "Escolaridade",
        Trabalha ~ "Trabalha",
        Idade ~ "Idade (anos)",
        Dificuldade_Financeira ~ "Dificuldade financeira"
  )                 
) %>%
    modify_spanning_header(all_stat_cols() ~ "**Alteração de hábitos alimentares na pandemia**") %>%
    modify_header(label ~ "**Variáveis**") %>%
    bold_labels() %>%
    modify_footnote(all_stat_cols() ~ "Variáveis qualitativas: n (%); Variáveis quantitativas: média (DP).") %>%
  add_p(
    test = list(all_categorical() ~ "chisq.test"),
    pvalue_fun = ~ style_pvalue(.x, digits = 3)
  )


```


Sobre a alteração de hábitos alimentares na pandemia:

- A maior parte das variáveis apresenta p-valor menor do que 0.05, indicando que essas variáveis são importantes para explicar as mudanças de hábito na pandemia

  - (Genero) As mulheres mudaram mais seus hábitos alimentares do que os homens.
  - (Renda_familiar) Famílias com renda de até R$ 1254,00 apresentaram maior mudança de hábitos e famílias com renda maior do que R$8640,00 apresentaram uma maior proporção do preenchimento de 'Não mudança de hábito'.
  - (Escolaridade) Altas escolaridades apresentaram maior proporção de não mudança de hábito e baixas escolaridades apresentaram maior proporção de mudança de hábito.
  - (Trabalha)Quem não trabalha teve maior mudança de hábito.
  - (Dificuldade_Financeira) Quem passou por dificuldade financeira apresentou maior mudança de hábito. Quem não passou por dificuldade financeira não passou por mudança de hábito.

### Filtrando somente que declarou alteração de hábito na pandemia e selecionando as variáveis

Aqui vou supor que quem respondeu 'Alteracao_Pandemia = Não' deveria ter preenchido nas variáveis associadas 'Não alterou

```{r, echo=TRUE, warning=FALSE, message=FALSE}
variaveis_estudo = c(desfechos, 'Genero', 'Renda_familiar','Escolaridade','Trabalha','Dificuldade_Financeira')

dados = dados0 %>% 
  filter(Alteracao_Pandemia == "Sim") %>%
  select(all_of(variaveis_estudo))
```


# Próximo passo: considerando quem mudou de hábito, essa mudança foi para bons hábitos ou maus hábitos? Qual a relação desses bons/maus hábitos com as variáveis da seção anterior? Exemplo: famílias de mais baixa renda passaram a consumir mais embutidos e industrializados? As mulheres que mudaram de hábito passaram a ter uma alimentação mais saudável? Pessoas que não trabalhavam passaram a consumir mais alcool? Etc.

Resultados gerados abaixo:

- Tabela descritiva das variáveis explicativas escolhidas - ok
- Tabela descritiva das variáveis desfecho escolhidas - ok
- Gráfico de barras das variáveis desfecho escolhidas - ok
- Gráfico de barras 2 a 2 das variáveis explicativas e desfecho - ok
- Criar cluster com as variáveis dos hábitos e analisar os clusters com as variáveis explicativas
- Criação de um escore para avaliar individualmente se a alimentação da pessoa melhorou ou piorou e relacionar com as variáveis explicativas - ok

  - Dar uma atenção na interpretação da variável Dificuldade_Financeira



## Caracterizar o perfil sociodemográfico

`r nrow(dados)` das `r nrow(dados0)` pessoas que responderam ao questionário declararam que alteraram hábitos alimentares durante a pandemia. Esse é o público alvo desse estudo.

```{r warning=FALSE, message=FALSE}
table1 <- dados %>%
  select(
    Genero, Renda_familiar, Escolaridade, Trabalha, Dificuldade_Financeira
  ) %>%
  tbl_summary(
    statistic = list(
      all_categorical() ~ "{n} ({p}%)",
      all_continuous() ~ "{mean} ({sd}) [{min} - {max}]"
    ),
    digits = list(
      all_continuous() ~ 2,
      all_categorical() ~ c(0, 2)
    ),
    label = list(
      Genero ~ "Gênero",
      Renda_familiar ~ "Renda Familiar",
      Escolaridade ~ "Escolaridade",
      Trabalha ~ "Trabalha",
      Dificuldade_Financeira ~ "Dificuldade Financeira"
    ),
    missing = "no"
  ) %>%
  bold_labels()

table1
```

```{r warning=FALSE, message=FALSE}
table2 <- dados %>%
  select(
    Frutas, Legumes_Verduras, Bebidas_Alcoolicas,
    Produtos_Industrializados, Doces
  ) %>%
  tbl_summary(
    statistic = all_categorical() ~ "{n} ({p}%)",
    digits = all_categorical() ~ c(0, 2),
    label = list(
      Frutas ~ "Frutas",
      Legumes_Verduras ~ "Legumes e Verduras",
      Bebidas_Alcoolicas ~ "Bebidas Alcoólicas",
      Produtos_Industrializados ~ "Produtos Industrializados",
      Doces ~ "Doces"
    ),
    missing = "no"
  ) %>%
  bold_labels()

table2
```

```{r echo=TRUE, warning=FALSE, message=FALSE}
jama_colors <- c('#374e55', '#df8f44', '#00a1d5', '#b24745')

labels_desfechos <- list(
  Frutas = "Frutas",
  Legumes_Verduras = "Legumes e Verduras",
  Bebidas_Alcoolicas = "Bebidas Alcoólicas",
  Produtos_Industrializados = "Produtos Industrializados",
  Consumo_agua = "Consumo de Água",
  Embutidos = "Embutidos"
)

grafico_distribuicao <- function(df, col, labels_desfechos = list(), colors = jama_colors) {
    
  col_label <- if (col %in% names(labels_desfechos)) labels_desfechos[[col]] else col
  
  grafico <- ggplot(df, aes(x = .data[[col]], fill = .data[[col]])) +
    geom_bar() +
    stat_count(
      geom = "text",
      aes(label = after_stat(count)),
      vjust = -0.5,
      size = 4,
      color = "black"
    ) +
    scale_fill_manual(values = colors) +
    scale_y_continuous(expand = expansion(mult = c(0, 0.13))) +
    labs(
      title = paste("Consumo de", col_label),
      x = col_label,
      y = "Contagem"
    ) +
    theme_bw() +
    theme(
      axis.text.x = element_text(angle = 45, hjust = 1),
      legend.position = "none"
    )
  
  return(grafico)
}
```

```{r warning=FALSE, message=FALSE}
grafico_distribuicao(
    dados,
    'Frutas',
    labels_desfechos
)

```

```{r warning=FALSE, message=FALSE}
grafico_distribuicao(
    dados,
    'Legumes_Verduras',
    labels_desfechos
)

```

```{r warning=FALSE, message=FALSE}
grafico_distribuicao(
    dados,
    'Bebidas_Alcoolicas',
    labels_desfechos
)

```

```{r warning=FALSE, message=FALSE}
grafico_distribuicao(
    dados,
    'Produtos_Industrializados',
    labels_desfechos
)

```

```{r warning=FALSE, message=FALSE}
grafico_distribuicao(
    dados,
    'Doces',
    labels_desfechos
)

```




```{r}
library(webr)           

PieDonut(data=dados,
          mapping=aes(pies=Renda_familiar,
                  donut = Bebidas_Alcoolicas),
          r0=0.4,
          showPieName = F,
          pieLabelSize = 4)
```

- Podemos observar que quanto maior a renda, maior foi o aumento no consumo de alcool


```{r}
library(ggstats)

ggplot(dados, aes(x = Genero, fill = Doces, by= Genero)) +
  geom_bar(position = "fill") +
  geom_text(stat = "prop", position = position_fill(.5)) +
  scale_fill_brewer(name="Satisfação com a\nocomida e bebida",palette = "Dark2")+
  scale_y_continuous(labels = scales::percent) +
  xlab("Gênero")+
  ylab("Proporção relativa (%)") +
  theme_bw()+
  theme(text = element_text(size = 14))
```


```{r}
library(tidyverse)

explicativas <- c('Genero', 'Renda_familiar', 'Escolaridade', 'Trabalha', 'Dificuldade_Financeira')
desfechos    <- c('Frutas', 'Legumes_Verduras', 'Bebidas_Alcoolicas', 'Produtos_Industrializados', 'Doces')

resultados <- expand.grid(Explicativa = explicativas, Desfecho = desfechos, stringsAsFactors = FALSE) %>%
  rowwise() %>%
  mutate(
    p_valor = round(chisq.test(table(dados[[Explicativa]], dados[[Desfecho]]))$p.value, 5)
  ) %>%
  ungroup() %>%
  arrange(p_valor)

kable(resultados)
```

# Gráficos 2 a 2

```{r}
library(ggstats)

# Filtrar relações significativas e ordenar pela sequência da lista 'explicativas'
relacoes_significativas <- resultados %>%
  #filter(p_valor < 0.05) %>%
  mutate(Explicativa = factor(Explicativa, levels = explicativas)) %>%
  arrange(Explicativa, p_valor) # Ordena por Explicativa e depois pelo p-valor dentro de cada grupo

# Função para criar o gráfico padronizado
plot_bivariado <- function(exp, desf) {
  exp <- as.character(exp)
  
  ggplot(dados, aes(x = .data[[exp]], fill = .data[[desf]], by = .data[[exp]])) +
    geom_bar(position = "fill") +
    geom_text(stat = "prop", position = position_fill(0.5), size = 3.5) +
    scale_fill_brewer(palette = "Set2", name = desf) +
    scale_y_continuous(labels = scales::percent) +
    labs(
      title = paste(desf, "por", exp),
      x = exp,
      y = "Proporção (%)"
    ) +
    theme_minimal() +
    theme(axis.text.x = element_text(angle = 45, hjust = 1))
}

# Gerar lista com os gráficos significativos ordenados
graficos <- map2(relacoes_significativas$Explicativa, 
                 relacoes_significativas$Desfecho, 
                 plot_bivariado)
```



```{r}
for(i in 1:length(graficos)){
  print(graficos[[i]])
}
```




```{r}
calcular_escore_alimentar <- function(dados,
                                      cols_saudaveis = c("Frutas", "Legumes_Verduras"),
                                      cols_nao_saudaveis = c("Bebidas_Alcoolicas", "Produtos_Industrializados", "Doces"),
                                      rotulo_aumentou = "Aumentou",
                                      rotulo_diminuiu = "Diminuiu",
                                      rotulos_sem_mudanca = c("Não alterou", "Não tenho o hábito")) {
  
  dados_proc <- dados
  
  # Mapeamento para alimentos saudáveis (+1 aumento, -1 redução, 0 sem mudança)
  pontuar_saudavel <- function(x) {
    ifelse(x == rotulo_aumentou, 1,
    ifelse(x == rotulo_diminuiu, -1,
    ifelse(x %in% rotulos_sem_mudanca, 0, NA)))
  }
  
  # Mapeamento para alimentos não saudáveis (-1 aumento, +1 redução, 0 sem mudança)
  pontuar_nao_saudavel <- function(x) {
    ifelse(x == rotulo_aumentou, -1,
    ifelse(x == rotulo_diminuiu, 1,
    ifelse(x %in% rotulos_sem_mudanca, 0, NA)))
  }
  
  # Aplica a pontuação coluna por coluna
  matriz_saudavel <- sapply(dados_proc[, cols_saudaveis, drop = FALSE], pontuar_saudavel)
  matriz_nao_saudavel <- sapply(dados_proc[, cols_nao_saudaveis, drop = FALSE], pontuar_nao_saudavel)
  
  # Calcula a soma das pontuações por indivíduo
  dados_proc$Escore_Continuo <- rowSums(cbind(matriz_saudavel, matriz_nao_saudavel), na.rm = TRUE)
  
  # Cria a variável categórica para a Regressão Logística
  dados_proc$Escore_Categoria <- factor(
    ifelse(dados_proc$Escore_Continuo > 0, "Melhorou",
    ifelse(dados_proc$Escore_Continuo < 0, "Piorou", "Manteve")),
    levels = c("Piorou", "Manteve", "Melhorou"),
    ordered = TRUE
  )
  
  return(dados_proc)
}

# ------------------------------------------------------------------------------
# EXEMPLO DE USO 1:
# ------------------------------------------------------------------------------
dados <- calcular_escore_alimentar(dados)
table(dados$Escore_Categoria) # Exibe contagem dos grupos
```




```{r}
ajustar_regressao_multivariada <- function(dados,
                                           var_resposta = "Escore_Categoria",
                                           preditores = c("Genero", "Renda_familiar", "Escolaridade", "Trabalha", "Dificuldade_Financeira")) {
  
  # Instala e carrega o pacote MASS se necessário
  if (!requireNamespace("MASS", quietly = TRUE)) install.packages("MASS")
  
  # Garantir que a variável dependente é um fator ordenado
  dados[[var_resposta]] <- factor(dados[[var_resposta]], levels = c("Piorou", "Manteve", "Melhorou"), ordered = TRUE)
  
  # Garantir que os preditores categóricos sejam fatores
  for (p in preditores) {
    if (is.character(dados[[p]])) {
      dados[[p]] <- as.factor(dados[[p]])
    }
  }
  
  # Monta a fórmula dinamicamente
  formula_modelo <- as.formula(paste(var_resposta, "~", paste(preditores, collapse = " + ")))
  
  # Ajusta o modelo Proportional Odds Logistic Regression (polr)
  modelo <- MASS::polr(formula_modelo, data = dados, Hess = TRUE)
  
  # Extração dos parâmetros
  sumario <- summary(modelo)
  n_coef <- length(coef(modelo))
  
  # Nomes dos coeficientes dos preditores (exclui os interceptos/pontos de corte)
  nomes_coef <- names(coef(modelo))
  
  # Cálculo dos p-valores baseados na distribuição normal (Wald test)
  t_values <- sumario$coefficients[nomes_coef, "t value"]
  p_valores <- 2 * (1 - pnorm(abs(t_values)))
  
  # Cálculo das Razões de Chances (OR = exp(beta)) e Intervalos de Confiança 95%
  or <- exp(coef(modelo))
  ci <- exp(confint.default(modelo)[nomes_coef, , drop = FALSE])
  
  # Tabela final consolidada
  tabela_or <- data.frame(
    Variavel = nomes_coef,
    Odds_Ratio = round(or, 3),
    IC_95_Inf = round(ci[, 1], 3),
    IC_95_Sup = round(ci[, 2], 3),
    p_valor = round(p_valores, 4)
  )
  
  rownames(tabela_or) <- NULL
  
  return(list(
    modelo_fit = modelo,
    tabela_resultados = tabela_or
  ))
}

# ------------------------------------------------------------------------------
# EXEMPLO DE USO 2:
# ------------------------------------------------------------------------------
# # 1. Garanta que calculou o escore primeiro:
# meudados <- calcular_escore_alimentar(dados)
#
# # 2. Execute a regressão multivariada:
resultado_regressao <- ajustar_regressao_multivariada(dados)
#
# # 3. Visualize a tabela de Odds Ratios:
print(resultado_regressao$tabela_resultados)
```





```{r}
dados0 <- read_excel("C:\\Users\\Usuario\\Desktop\\DataProjects\\quarto_archives\\DadosNutri.xlsx")

dados = dados0 %>% 
  filter(Alteracao_Pandemia == "Sim")


# 1. Carregar os pacotes
library(tidymodels)
library(tidyclust) # Extensão do tidymodels para clusterização

set.seed(2026)
# 2. Dados de exemplo (removendo a variável categórica do dataset iris)
dados_clust <- dados %>%
  select(Frutas, Legumes_Verduras, Bebidas_Alcoolicas, Produtos_Industrializados, Doces) %>%
  mutate(across(everything(), ~ case_when(
    . == "Não tenho o hábito" ~ 0,
    . == "Diminuiu"           ~ 1,
    . == "Não alterou"        ~ 2,
    . == "Aumentou"           ~ 3,
    TRUE                      ~ NA_real_
  )))

# 3. Criar a receita de pré-processamento (K-Means exige variáveis normalizadas)
rec_kmeans <- recipe(~ ., data = dados_clust) %>%
  step_normalize(all_numeric_predictors())

# 4. Especificar o modelo KMeans (ex: k = 3 clusters)
spec_kmeans <- k_means(num_clusters = 3) %>%
  set_engine("stats")

# 5. Criar o Workflow e ajustar o modelo
wf_kmeans <- workflow() %>%
  add_recipe(rec_kmeans) %>%
  add_model(spec_kmeans)

fit_kmeans <- wf_kmeans %>% fit(data = dados_clust)

# 6. Extrair resultados e atribuições de clusters
# Adiciona a coluna com a atribuição do cluster a cada observação
dados_com_clusters <- augment(fit_kmeans, new_data = dados_clust)
head(dados_com_clusters)

# Visualizar os centroides de cada cluster (nas escalas normalizadas)
extract_centroids(fit_kmeans)


dados = dados %>%
  bind_cols(dados_com_clusters %>% select(.pred_cluster))

tbl_summary(
    data = dados #%>%
      #select(all_of(c('Genero', 'Renda_familiar','Escolaridade','Trabalha','Dificuldade_Financeira')), .pred_cluster)
    ,
    by = ".pred_cluster",
    statistic = list(all_categorical() ~ "{n} ({p}%)",
                     all_continuous() ~ "{mean} ({sd})")              
)



cluster 1: (aumentou o consumo de todo dia de alimento)
  Aumentou: Frutas, Legumes_Verduras, Bebidas_Alcoolicas, Doces
  Não alterou: Produtos_Industrializados
  Diminuiu: 

cluster 2: (aumentou o consumo de alimentos não-saudáveis)
  Aumentou: Bebidas_Alcoolicas, Produtos_Industrializados, Doces
  Não alterou: 
  Diminuiu: Frutas, Legumes_Verduras

cluster 3: (aumentou o consumo de alimentos saudáveis)
  Aumentou: Frutas, Legumes_Verduras
  Não alterou: 
  Diminuiu: Bebidas_Alcoolicas, Produtos_Industrializados, Doces

Próximo passo: analisar as variáveis explicativas de cada cluster


```